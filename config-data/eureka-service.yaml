server:
  port: 8761
eureka:
  instance: 
    hostname: localhost
  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl: http://${eureka.instance.hostname}:${server.port}/eureka/